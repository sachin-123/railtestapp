<%= form_for(@demoapp) do |f| %>
  <% if @demoapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demoapp.errors.count, "error") %> prohibited this demoapp from being saved:</h2>

      <ul>
      <% @demoapp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :pass %><br />
    <%= f.text_field :pass %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
